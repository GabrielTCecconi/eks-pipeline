{
  "name": "hello-eks",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "health": "curl -fsS http://localhost:${PORT:-3000}/healthz || exit 1"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
